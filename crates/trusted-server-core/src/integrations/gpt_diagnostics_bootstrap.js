// GPT diagnostics activation is server-owned and is transported only through
// the validated, frozen diagnostics boot value. This intentionally has no
// browser-side activation behavior and remains only until the wiring cutover
// removes the superseded asset.

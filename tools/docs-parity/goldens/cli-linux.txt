=== ts ===
Trusted Server CLI

Usage: ts <COMMAND>

Commands:
  active-version  Print the currently active deployment version for a target adapter
  audit           Browser-backed page and ad-template audits
  auth            Sign in / out / status against an `EdgeZero` adapter
  build           Build the project for a target adapter
  config          Trusted Server app-config commands
  deploy          Deploy the project through a target adapter
  healthcheck     Probe a deployed version until it reports healthy
  prebid          Trusted Server Prebid commands
  provision       Provision platform resources through a target adapter
  rollback        Roll a service back to a previously active deployment version
  serve           Serve the project locally through a target adapter
  dev             Local developer tools (e.g. the macOS-only production-hostname proxy)
  help            Print this message or the help of the given subcommand(s)

Options:
  -h, --help     Print help
  -V, --version  Print version

=== ts active-version ===
Print the currently active deployment version for a target adapter

Usage: ts active-version --adapter <ADAPTER> --service-id <SERVICE_ID>

Options:
      --adapter <ADAPTER>        Target adapter name
      --service-id <SERVICE_ID>  Platform service id whose active version to resolve. Required
  -h, --help                     Print help

=== ts audit ===
Browser-backed page and ad-template audits

Usage: ts audit [COMMAND]

Commands:
  page          Audit a single page and print a read-only summary
  ad-templates  Verify configured ad-template slots against live page evidence
  generate      Bootstrap a draft Trusted Server config + JS asset audit from a live page
  help          Print this message or the help of the given subcommand(s)

Options:
  -h, --help  Print help

=== ts auth ===
Sign in / out / status against an `EdgeZero` adapter

Usage: ts auth <COMMAND>

Commands:
  login   Sign in (`wrangler login` / `fastly profile create` / `spin cloud login`)
  logout  Sign out (`wrangler logout` / `fastly profile delete` / `spin cloud logout`)
  status  Show the current session (`wrangler whoami` / `fastly profile list` / `spin cloud info`)
  help    Print this message or the help of the given subcommand(s)

Options:
  -h, --help  Print help

=== ts build ===
Build the project for a target adapter

Usage: ts build --adapter <ADAPTER> [ADAPTER_ARGS]...

Arguments:
  [ADAPTER_ARGS]...  Arguments passed through to the adapter build command

Options:
      --adapter <ADAPTER>  Target adapter name
  -h, --help               Print help

=== ts config ===
Trusted Server app-config commands

Usage: ts config <COMMAND>

Commands:
  ad-templates  Diagnose server-side ad-template configuration and path matching
  init          Initialize a Trusted Server config file from the example template
  diff          Diff `trusted-server.toml` against the live `EdgeZero` config
  gc            Reclaim orphaned chunk entries leaked from prior oversized pushes
  push          Push `trusted-server.toml` as a blob envelope through `EdgeZero`
  validate      Validate `edgezero.toml` and the typed Trusted Server config
  help          Print this message or the help of the given subcommand(s)

Options:
  -h, --help  Print help

=== ts deploy ===
Deploy the project through a target adapter

Usage: ts deploy [OPTIONS] --adapter <ADAPTER> [-- <ADAPTER_ARGS>...]

Arguments:
  [ADAPTER_ARGS]...  Passthrough args for the adapter deploy command. These MUST follow a `--` separator: an unknown hyphenated token before `--` is rejected, not captured here. This is a safety boundary — otherwise a stray `--stage` (or any typo'd flag) would slip through as passthrough, leave `--staging` false, and route a staging-intended deploy to PRODUCTION

Options:
      --adapter <ADAPTER>        Target adapter name
      --service-id <SERVICE_ID>  Platform service id the deploy targets. Consumed by the Fastly staging lifecycle: production deploy passes it through to `fastly compute deploy` and resolves the activated version; `--staging` uses it to clone + stage a draft version. Adapters that don't need a service id ignore it
      --staging                  Stage a draft version instead of activating (Fastly only): builds and uploads to a new draft service version cloned from the active one, marks it staged, and emits the staged version. Non-Fastly adapters reject `--staging`. The same `--staging` verb healthcheck/rollback/config-push/config-diff use
  -h, --help                     Print help

=== ts dev ===
Local developer tools (e.g. the macOS-only production-hostname proxy)

Usage: ts dev

Options:
  -h, --help  Print help

=== ts healthcheck ===
Probe a deployed version until it reports healthy

Usage: ts healthcheck [OPTIONS] --adapter <ADAPTER> --domain <DOMAIN> --service-id <SERVICE_ID> --version <VERSION>

Options:
      --adapter <ADAPTER>          Target adapter name
      --domain <DOMAIN>            Public domain to probe. Required: the deploy→healthcheck→rollback contract always threads it
      --path <PATH>                URL path to probe on the domain (must begin with '/'). Applies to production and staging alike — staging reroutes the same URL to the resolved staging IP. Defaults to '/' [default: /]
      --retry <RETRY>              Total number of attempts before declaring the probe unhealthy [default: 3]
      --retry-delay <RETRY_DELAY>  Seconds to wait between attempts [default: 5]
      --service-id <SERVICE_ID>    Platform service id to probe. Required (staging resolves the staging IP from it; production threads it for parity)
      --staging                    Probe the staged version via its resolved staging IP rather than the live production endpoint
      --timeout <TIMEOUT>          Per-attempt connect/read timeout in seconds [default: 10]
      --version <VERSION>          Service version to probe (threaded from a prior deploy/stage). Required so the version is never silently dropped
  -h, --help                       Print help

=== ts prebid ===
Trusted Server Prebid commands

Usage: ts prebid <COMMAND>

Commands:
  bundle  Generate a local external Prebid bundle and update config metadata
  help    Print this message or the help of the given subcommand(s)

Options:
  -h, --help  Print help

=== ts provision ===
Provision platform resources through a target adapter

Usage: ts provision [OPTIONS] --adapter <ADAPTER>

Options:
      --adapter <ADAPTER>    Target adapter name
      --dry-run              Print the would-be commands and would-be manifest edits without performing them
      --manifest <MANIFEST>  Path to the manifest (default: `edgezero.toml`) [default: edgezero.toml]
  -h, --help                 Print help

=== ts rollback ===
Roll a service back to a previously active deployment version

Usage: ts rollback [OPTIONS] --adapter <ADAPTER> --service-id <SERVICE_ID> --version <VERSION>

Options:
      --adapter <ADAPTER>          Target adapter name
      --rollback-to <ROLLBACK_TO>  Production only: the version to re-activate. Fastly exposes no metadata to tell a previously-live version from a staged one, so the rollback target CANNOT be inferred; it is captured before the deploy that superseded it (see `deploy`'s `previous-version`) and passed here. Required for a production rollback; ignored for staging
      --service-id <SERVICE_ID>    Platform service id to roll back. Required
      --staging                    Roll back the staged version (deactivate) instead of the production version (activate `--rollback-to`)
      --version <VERSION>          The current (bad) version. Staging deactivates it. Required
  -h, --help                       Print help

=== ts serve ===
Serve the project locally through a target adapter

Usage: ts serve --adapter <ADAPTER>

Options:
      --adapter <ADAPTER>  Target adapter name
  -h, --help               Print help

=== ts audit ad-templates ===
Verify configured ad-template slots against live page evidence

Usage: ts audit ad-templates <COMMAND>

Commands:
  generate  Scrape a live page's GPT slots and update the config's `[creative_opportunities]` slots in place
  verify    Verify ad-template slots for one or more live URLs
  help      Print this message or the help of the given subcommand(s)

Options:
  -h, --help  Print help

=== ts audit generate ===
Bootstrap a draft Trusted Server config + JS asset audit from a live page

Usage: ts audit generate [OPTIONS] <URL>

Arguments:
  <URL>
          Public HTTP(S) URL to audit

Options:
      --js-assets <JS_ASSETS>
          JavaScript asset audit output path

      --config <CONFIG>
          Draft Trusted Server config output path

      --no-js-assets
          Do not write the JavaScript asset audit file

      --no-config
          Do not write the draft Trusted Server config file

      --force
          Overwrite existing output files

      --cookie <NAME=VALUE>
          Cookie to send with the page request, as `name=value`. Repeatable. Use to carry an existing session (e.g. a valid bot-protection clearance cookie) so the origin serves the real page instead of a challenge

      --chrome <CHROME>
          Path to the Chrome/Chromium executable. Falls back to `$CHROME`, then auto-detection

      --headful
          Run a visible browser instead of Chrome's new headless mode

      --no-assume-consent
          Do not answer the standard IAB consent APIs for the fresh audit profile

      --browser-proxy <HOST:PORT>
          Route the browser through this proxy, as `host:port` or a full URL

      --settle-quiet-ms <SETTLE_QUIET_MS>
          Quiet window in milliseconds that marks the page settled
          
          [default: 750]

      --settle-max-ms <SETTLE_MAX_MS>
          Hard cap in milliseconds on waiting for the page to settle
          
          [default: 12000]

      --danger-accept-invalid-certs
          Navigate to origins whose TLS certificate does not validate.
          
          DANGEROUS: the audit sends any `--cookie` session to the origin and treats what it reads back as the evidence it writes config from, so an invalid certificate could mean an impersonator is harvesting the session and fabricating the evidence. Use only against a host you control with a known self-signed certificate.

  -h, --help
          Print help (see a summary with '-h')

=== ts audit page ===
Audit a single page and print a read-only summary

Usage: ts audit page [OPTIONS] <URL>

Arguments:
  <URL>
          The page URL to audit (http or https)

Options:
      --scroll
          Perform a deterministic scroll pass after the initial settle

      --chrome <CHROME>
          Path to the Chrome/Chromium executable. Falls back to `$CHROME`, then auto-detection on `PATH` and standard install locations

      --browser-profile <PROFILE>
          Browser device profile used for viewport and user-agent emulation

          Possible values:
          - desktop: Desktop Chrome at 1280×800
          - mobile:  Mobile-sized viewport with a mobile user agent
          
          [default: desktop]

      --headful
          Run a visible browser instead of Chrome's new headless mode

      --no-assume-consent
          Do not answer the standard IAB consent APIs for the fresh audit profile

      --browser-proxy <HOST:PORT>
          Route the browser through this proxy, as `host:port` or a full URL

      --settle-quiet-ms <SETTLE_QUIET_MS>
          Quiet window in milliseconds (no new network resources) that marks the page settled
          
          [default: 750]

      --settle-max-ms <SETTLE_MAX_MS>
          Hard cap in milliseconds on waiting for the page to settle
          
          [default: 10000]

      --danger-accept-invalid-certs
          Navigate to origins whose TLS certificate does not validate.
          
          DANGEROUS: the audit sends any `--cookie` session to the origin and treats what it reads back as verification evidence, so an invalid certificate could mean an impersonator is harvesting the session and fabricating the evidence. Use only against a host you control with a known self-signed certificate.

  -h, --help
          Print help (see a summary with '-h')

=== ts auth login ===
Sign in (`wrangler login` / `fastly profile create` / `spin cloud login`)

Usage: ts auth login --adapter <ADAPTER>

Options:
      --adapter <ADAPTER>  
  -h, --help               Print help

=== ts auth logout ===
Sign out (`wrangler logout` / `fastly profile delete` / `spin cloud logout`)

Usage: ts auth logout --adapter <ADAPTER>

Options:
      --adapter <ADAPTER>  
  -h, --help               Print help

=== ts auth status ===
Show the current session (`wrangler whoami` / `fastly profile list` / `spin cloud info`)

Usage: ts auth status --adapter <ADAPTER>

Options:
      --adapter <ADAPTER>  
  -h, --help               Print help

=== ts config ad-templates ===
Diagnose server-side ad-template configuration and path matching

Usage: ts config ad-templates <COMMAND>

Commands:
  lint     Validate ad-template config and summarize deploy-time implications
  match    Show creative opportunity slots matching a page path or URL
  check    Assert that a page path or URL matches the expected slot set
  explain  Explain why a page path or URL would or would not run the ad stack
  help     Print this message or the help of the given subcommand(s)

Options:
  -h, --help  Print help

=== ts config diff ===
Diff `trusted-server.toml` against the live `EdgeZero` config

Usage: ts config diff [OPTIONS] --adapter <ADAPTER>

Options:
      --adapter <ADAPTER>
          Target adapter name

      --app-config <APP_CONFIG>
          Path to the typed app-config file (default: `<app_name>.toml` resolved from the manifest's `[app].name`, next to the manifest)

      --exit-code
          Exit with a non-zero code when changes exist (for CI gating)

      --format <FORMAT>
          Output format for the diff

          Possible values:
          - json:       Machine-readable JSON object with `local_sha256`, `remote_sha256`, `added`, `removed`, `changed` fields
          - structured: Machine-readable structured representation (key/old/new triples)
          - unified:    POSIX unified-diff text (default)
          
          [default: unified]

      --key <KEY>
          Override the default key

      --local
          Diff against the adapter's local-emulator state instead of the live platform

      --manifest <MANIFEST>
          Path to the manifest (default: `edgezero.toml`)
          
          [default: edgezero.toml]

      --no-env
          Skip the `<APP_NAME>__…__<KEY>` env-var overlay when loading the typed app-config

      --runtime-config <RUNTIME_CONFIG>
          Path to the adapter's runtime configuration file

      --staging
          Diff against the staging key (`<logical-id>_staging`) in the same store, so a staged diff compares exactly what `config push --staging` would write. Mutually exclusive with `--key`, for the same reason as on push

      --store <STORE>
          Logical config store id to diff against. Defaults to the `[stores.config].default` (or the only declared id when `[stores.config].ids` has length 1)

  -h, --help
          Print help (see a summary with '-h')

=== ts config gc ===
Reclaim orphaned chunk entries leaked from prior oversized pushes

Usage: ts config gc [OPTIONS] --adapter <ADAPTER>

Options:
      --adapter <ADAPTER>
          Adapter whose config store to reclaim (e.g. `fastly`)

      --dry-run
          Preview only: report what `gc` WOULD delete and delete nothing. This is already the default (a run without `--yes` never deletes); the flag just lets you state that intent explicitly to double-check a sweep. It CONFLICTS with `--yes` -- a single run cannot both preview and delete

      --manifest <MANIFEST>
          Path to `edgezero.toml`
          
          [default: edgezero.toml]

      --no-env
          Ignore `EDGEZERO__STORES__CONFIG__<ID>__NAME` when resolving which PHYSICAL store to sweep, so the logical id `<ID>` is used as the physical store name instead.
          
          This is NOT the app-config overlay the other `config` subcommands mean by `--no-env` — `gc` never loads your typed app config. On a DESTRUCTIVE command it selects a DIFFERENT TARGET: if that variable is what maps your logical id onto the real store, `--no-env` points `gc` at a different store (or one that does not exist). Check the store id `gc` reports before passing `--yes`.

      --older-than <OLDER_THAN>
          Only reclaim entries older than this. This is YOUR SAFETY ASSERTION, and it is about the WHOLE PHYSICAL STORE, not just one config: `gc` sweeps every root in the selected store, so you are asserting "NO root in this store changed within this window, and no writer is targeting it" — so nothing superseded more recently (which POPs may still serve) is deleted. A sibling root you re-pushed minutes ago is enough to make a wide window unsafe, especially if it changed to a value small enough to store directly (that leaves no chunk for `gc` to date it by). Accepts `s`/`m`/`h`/`d` suffixes (e.g. `7d`, `24h`, `90m`); a bare number means seconds, and 0 is rejected for `--yes`. REQUIRED for `--yes` (a destructive run must not guess it); a dry-run without it previews every orphan and its age

      --store <STORE>
          Override the config-store id (defaults to the manifest's)

      --yes
          Actually delete. Without this, `gc` only reports what it WOULD delete

  -h, --help
          Print help (see a summary with '-h')

=== ts config init ===
Initialize a Trusted Server config file from the example template

Usage: ts config init [OPTIONS]

Options:
      --app-config <APP_CONFIG>  Target app-config path [default: trusted-server.toml]
      --force                    Overwrite an existing target file
  -h, --help                     Print help

=== ts config push ===
Push `trusted-server.toml` as a blob envelope through `EdgeZero`

Usage: ts config push [OPTIONS] --adapter <ADAPTER>

Options:
      --adapter <ADAPTER>
          Target adapter name
      --app-config <APP_CONFIG>
          Path to the typed app-config file (default: `<app_name>.toml` resolved from the manifest's `[app].name`, next to the manifest)
      --dry-run
          Print the would-be operations without performing them
      --key <KEY>
          Override the default key
      --local
          Push to the adapter's local-emulator state instead of the live platform. For Fastly this edits `[local_server.config_stores]` in the adapter's `fastly.toml` (Viceroy reads it on startup); for Cloudflare it runs `wrangler kv bulk put --local` so writes land in `.wrangler/state`. Axum's push is already local-only, so `--local` is a no-op there. For Spin, `--local` suppresses Fermyon Cloud auto-detection so the push writes directly to Spin's local `SQLite` KV file (`<spin.toml dir>/.spin/sqlite_key_value.db`) even when the manifest's deploy command shells to `spin deploy`
      --manifest <MANIFEST>
          Path to the manifest (default: `edgezero.toml`) [default: edgezero.toml]
      --no-diff
          Skip the inline diff render
      --no-env
          Skip the `<APP_NAME>__…__<KEY>` env-var overlay when loading the typed app-config. The default loads the overlay so the runtime and the push see the same resolved values
      --runtime-config <RUNTIME_CONFIG>
          Path to the adapter's runtime configuration file. Currently only honoured by Spin, which reads `[key_value_store.<label>]` stanzas to dispatch `config push --adapter spin` to the right backend writer (`type = "spin"` → direct `SQLite` write; redis/azure-*/etc. → errors pointing at the native backend CLI). Default: `runtime-config.toml` next to the adapter manifest
      --staging
          Push to staging: write the config under the `<logical-id>_staging` key in the SAME store, so it never overwrites the production key the live service reads. The same `--staging` verb `deploy`/`healthcheck`/`rollback` use. Mutually exclusive with `--key`: the staging key is derived from the store's logical id because that is what the staging selector store (created and linked by a staged deploy) points a staged version at, so an explicit key would be written where nothing reads it
      --store <STORE>
          Logical config store id to push to. Defaults to the `[stores.config].default` (or the only declared id when `[stores.config].ids` has length 1)
  -y, --yes
          Skip the inline diff prompt and write unconditionally
  -h, --help
          Print help

=== ts config validate ===
Validate `edgezero.toml` and the typed Trusted Server config

Usage: ts config validate [OPTIONS]

Options:
      --app-config <APP_CONFIG>  Path to the typed app-config file (default: `<app_name>.toml` resolved from the manifest's `[app].name`, next to the manifest)
      --manifest <MANIFEST>      Path to the manifest (default: `edgezero.toml`) [default: edgezero.toml]
      --no-env                   Skip the `<APP_NAME>__…__<KEY>` env-var overlay when loading the typed app-config. The default loads the overlay so validation sees the same values the runtime would
      --strict                   Strict mode: additionally check capability-aware completeness for the declared adapter set and well-formed handler paths
  -h, --help                     Print help

=== ts prebid bundle ===
Generate a local external Prebid bundle and update config metadata

Usage: ts prebid bundle [OPTIONS]

Options:
      --config <CONFIG>  Trusted Server config path [default: trusted-server.toml]
      --out <OUT>        Local output directory for generated Prebid bundle artifacts [default: dist/prebid]
  -h, --help             Print help

=== ts audit ad-templates generate ===
Scrape a live page's GPT slots and update the config's `[creative_opportunities]` slots in place

Usage: ts audit ad-templates generate [OPTIONS] <URL>

Arguments:
  <URL>
          Page URL to scrape for GPT slots (http or https)

Options:
      --app-config <APP_CONFIG>
          Path to `trusted-server.toml`. Defaults to `<app.name>.toml` beside `edgezero.toml`

      --manifest <MANIFEST>
          Path to `edgezero.toml`
          
          [default: edgezero.toml]

      --no-env
          Skip app-config environment overlay

      --page-pattern <GLOB>
          Glob applied to every slot discovered this run (e.g. `/`, `/news/*`). Repeatable. Defaults to the scraped URL's path. Re-running with a different pattern unions it into slots already in the config

      --replace
          Replace all existing slots instead of merging this run into them

      --dry-run
          Preview the updated config on stdout instead of writing it

      --scroll
          Perform a deterministic scroll pass after each page initially settles

      --cookie <NAME=VALUE>
          Cookie to send with the page request, as `name=value`. Repeatable. Use to carry an existing session (e.g. a valid bot-protection clearance cookie) so the origin serves the real page instead of a challenge

      --max-sections <MAX_SECTIONS>
          Maximum site sections to sample. Each contributes a landing page and an article, so this bounds how much of the publisher's taxonomy is covered
          
          [default: 8]

      --max-pages <MAX_PAGES>
          Maximum pages to load in total, including the requested page.
          
          Set to 1 to restore single-page behavior: no crawl, no section discovery, and the audited path as the only page pattern.
          
          [default: 17]

      --profiles <PROFILES>
          Device profiles to audit, comma-separated: `desktop`, `mobile`.
          
          Defaults to `desktop`. Publishers often serve different GAM ad units per device, which a single-profile crawl cannot see — it would infer a template correct for the profile it used and silently wrong elsewhere. Passing both crawls each page twice and refuses to write an ad-unit path for any slot where the profiles disagree.
          
          [default: desktop]

      --page-delay-ms <PAGE_DELAY_MS>
          Pause in milliseconds between page loads during the crawl.
          
          A crawl issues a dozen navigations in a row. Firing them back to back is discourteous to the origin, and request pacing is one of the signals bot protection scores, so an unpaced crawl can trigger the challenge that empties the rest of the run.
          
          [default: 750]

      --chrome <CHROME>
          Path to the Chrome/Chromium executable. Falls back to `$CHROME`, then auto-detection

      --headful
          Run a visible browser instead of Chrome's new headless mode

      --no-assume-consent
          Do not answer the standard IAB consent APIs for the fresh audit profile

      --browser-proxy <HOST:PORT>
          Route the browser through this proxy, as `host:port` or a full URL

      --settle-quiet-ms <SETTLE_QUIET_MS>
          Quiet window in milliseconds that marks the page settled
          
          [default: 750]

      --settle-max-ms <SETTLE_MAX_MS>
          Hard cap in milliseconds on waiting for the page to settle
          
          [default: 12000]

      --danger-accept-invalid-certs
          Navigate to origins whose TLS certificate does not validate.
          
          DANGEROUS: the audit sends any `--cookie` session to the origin and treats what it reads back as the evidence it writes config from, so an invalid certificate could mean an impersonator is harvesting the session and fabricating the evidence. Use only against a host you control with a known self-signed certificate.

  -h, --help
          Print help (see a summary with '-h')

=== ts audit ad-templates verify ===
Verify ad-template slots for one or more live URLs

Usage: ts audit ad-templates verify [OPTIONS] <URLS>...

Arguments:
  <URLS>...
          One or more page URLs to verify (http or https)

Options:
      --app-config <APP_CONFIG>
          Path to `trusted-server.toml`. Defaults to `<app.name>.toml` beside `edgezero.toml`

      --manifest <MANIFEST>
          Path to `edgezero.toml`
          
          [default: edgezero.toml]

      --no-env
          Skip app-config environment overlay

      --strict
          Exit non-zero when a matched slot is missing or only partially confirmed

      --json
          Emit machine-readable JSON instead of human output

      --scroll
          Perform a deterministic scroll pass after the initial settle

      --allow-cross-origin-redirect
          Accept evidence from a page that redirected to a different origin.
          
          Off by default: slots are matched on the post-redirect path, so an off-origin page could otherwise satisfy `--strict`. Enable only for a known redirect between your own properties (e.g. apex to `www`).

      --cookie <NAME=VALUE>
          Cookie to send with each page request, as `name=value`. Repeatable. Use to carry an existing session (e.g. a valid bot-protection clearance cookie) so the origin serves the real page instead of a challenge

      --chrome <CHROME>
          Path to the Chrome/Chromium executable. Falls back to `$CHROME`, then auto-detection on `PATH` and standard install locations

      --browser-profile <PROFILE>
          Browser device profile used for viewport and user-agent emulation

          Possible values:
          - desktop: Desktop Chrome at 1280×800
          - mobile:  Mobile-sized viewport with a mobile user agent
          
          [default: desktop]

      --headful
          Run a visible browser instead of Chrome's new headless mode

      --no-assume-consent
          Do not answer the standard IAB consent APIs for the fresh audit profile

      --browser-proxy <HOST:PORT>
          Route the browser through this proxy, as `host:port` or a full URL

      --settle-quiet-ms <SETTLE_QUIET_MS>
          Quiet window in milliseconds (no new network resources) that marks the page settled
          
          [default: 750]

      --settle-max-ms <SETTLE_MAX_MS>
          Hard cap in milliseconds on waiting for the page to settle
          
          [default: 10000]

      --danger-accept-invalid-certs
          Navigate to origins whose TLS certificate does not validate.
          
          DANGEROUS: the audit sends any `--cookie` session to the origin and treats what it reads back as verification evidence, so an invalid certificate could mean an impersonator is harvesting the session and fabricating the evidence. Use only against a host you control with a known self-signed certificate.

  -h, --help
          Print help (see a summary with '-h')

=== ts config ad-templates check ===
Assert that a page path or URL matches the expected slot set

Usage: ts config ad-templates check [OPTIONS] <--expected-slot <ID>|--expect-no-slots> <PATH_OR_URL>

Arguments:
  <PATH_OR_URL>  Page path or full URL to evaluate

Options:
      --app-config <APP_CONFIG>  Path to `trusted-server.toml`. Defaults to `<app.name>.toml` beside `edgezero.toml`
      --manifest <MANIFEST>      Path to `edgezero.toml` [default: edgezero.toml]
      --no-env                   Skip app-config environment overlay
      --expected-slot <ID>       Expected slot id. Repeat for multiple slots
      --expect-no-slots          Assert that no slots match the page path or URL
      --allow-extra-slots        Allow additional matched slots beyond --expected-slot values
  -h, --help                     Print help

=== ts config ad-templates explain ===
Explain why a page path or URL would or would not run the ad stack

Usage: ts config ad-templates explain [OPTIONS] <PATH_OR_URL>

Arguments:
  <PATH_OR_URL>  Page path or full URL to evaluate

Options:
      --app-config <APP_CONFIG>  Path to `trusted-server.toml`. Defaults to `<app.name>.toml` beside `edgezero.toml`
      --manifest <MANIFEST>      Path to `edgezero.toml` [default: edgezero.toml]
      --no-env                   Skip app-config environment overlay
      --method <METHOD>          HTTP method to model [default: GET]
      --non-navigation           Model a non-navigation request
      --prefetch                 Model a prefetch request
      --bot                      Model a known crawler user agent
      --consent-denied           Model consent denying server-side auction
  -h, --help                     Print help

=== ts config ad-templates lint ===
Validate ad-template config and summarize deploy-time implications

Usage: ts config ad-templates lint [OPTIONS]

Options:
      --app-config <APP_CONFIG>  Path to `trusted-server.toml`. Defaults to `<app.name>.toml` beside `edgezero.toml`
      --manifest <MANIFEST>      Path to `edgezero.toml` [default: edgezero.toml]
      --no-env                   Skip app-config environment overlay
  -h, --help                     Print help

=== ts config ad-templates match ===
Show creative opportunity slots matching a page path or URL

Usage: ts config ad-templates match [OPTIONS] <PATH_OR_URL>

Arguments:
  <PATH_OR_URL>  Page path or full URL to evaluate

Options:
      --app-config <APP_CONFIG>  Path to `trusted-server.toml`. Defaults to `<app.name>.toml` beside `edgezero.toml`
      --manifest <MANIFEST>      Path to `edgezero.toml` [default: edgezero.toml]
      --no-env                   Skip app-config environment overlay
      --details                  Include slot div, GAM path, formats, and providers
  -h, --help                     Print help


/** @file Negative fixture for a noncanonical JSDoc type. */
void 0;

/**
 * Return the supplied value.
 * @param {String} value - Value to return.
 * @returns {String} The supplied value.
 */
export function fixtureValue(value: string): string {
  return value;
}
